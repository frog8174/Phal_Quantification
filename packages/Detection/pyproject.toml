[project]
name = "detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aaron", email = "chenz8174@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.6",
    "opencv-python>=4.12.0.88",
    "torch>=2.8.0",
    "ultralytics>=8.3.200",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
